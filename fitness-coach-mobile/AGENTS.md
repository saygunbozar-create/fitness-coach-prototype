# Expo HAS CHANGED

This project runs Expo **SDK 54** (downgraded from the 57 scaffold default to match the trainer's installed Expo Go client). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code, and keep `expo install --check` green when adding packages.
